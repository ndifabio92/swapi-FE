// interface Props {
//     data: any,
//     sortField: string,
//     ascending: boolean
// }

// export const sortedList = ({ data, sortField, ascending }: Props) => data?.sort((a, b) => {
//     if (ascending) return a[sortField]?.localeCompare(b[sortField]);
//     return b[sortField]?.localeCompare(a[sortField]);
// });